import { NAME } from './constants';

export const ADD_ACTIVITY = `${NAME}/add`;
export const UPDATE_ACTIVITY = `${NAME}/update`;
export const REMOVE_ACTIVITY = `${NAME}/remove`;
export const CLEAR_ACTIVITIES = `${NAME}/clear`;
